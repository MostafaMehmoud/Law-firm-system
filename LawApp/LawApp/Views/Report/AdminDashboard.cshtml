@using Law.CORE.ViewModels
@model AdminReportViewModel
@{
    ViewData["Title"] = "لوحة تقارير الإدارة";
}

<h2>📊 تقارير الإدارة</h2>

<div class="row">
    <div class="col-md-3">
        <div class="card bg-light p-3 mb-3 shadow-sm">
            <h5>📁 عدد القضايا</h5>
            <h4>@Model.TotalCases</h4>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-light p-3 mb-3 shadow-sm">
            <h5>📨 عدد العروض</h5>
            <h4>@Model.TotalOffers</h4>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-light p-3 mb-3 shadow-sm">
            <h5>💬 عدد الآراء</h5>
            <h4>@Model.TotalOpinions</h4>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-light p-3 mb-3 shadow-sm">
            <h5>👤 عدد المستخدمين</h5>
            <h4>@Model.TotalUsers</h4>
        </div>
    </div>
</div>

<hr />

<h4>📈 إحصاءات متقدمة</h4>
<ul class="list-group">
    <li class="list-group-item">✅ أكثر قضية حصلت على عروض: <strong>@Model.MostOfferedCaseTitle</strong> (@Model.MostOfferedCaseCount عرض)</li>
    <li class="list-group-item">✅ أكثر محامٍ قدم عروضًا: <strong>@Model.TopLawyerName</strong> (@Model.TopLawyerOfferCount عرض)</li>
    <li class="list-group-item">✅ أكثر قضية حصلت على آراء: <strong>@Model.MostOpinionatedCaseTitle</strong> (@Model.MostOpinionatedCaseCount رأي)</li>
</ul>
